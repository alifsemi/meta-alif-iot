# We have a conf and classes directory, add to BBPATH
BBPATH .= ":${LAYERDIR}"

# We have recipes-* directories, add to BBFILES
BBFILES += "${LAYERDIR}/recipes-*/*/*.bb \
            ${LAYERDIR}/recipes-*/*/*.bbappend"

BBFILE_COLLECTIONS += "meta-alif-ensemble-iot"
BBFILE_PATTERN_meta-alif-ensemble-iot = "^${LAYERDIR}/"
BBFILE_PRIORITY_meta-alif-ensemble-iot = "6"

LAYERDEPENDS_meta-alif-ensemble-iot = "core"
LAYERSERIES_COMPAT_meta-alif-ensemble-iot = "zeus"
